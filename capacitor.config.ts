import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.macj',
  appName: 'macj',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
